<footer>
  <small>
    <% if switched_from_admin? %>
      <%= link_to 'Switch back to account', admin_user_switch_back_path(session[:user_id]), method: :delete %>
    <% end %>
    <% if current_user %>
      <% if current_user.admin %>
        <%= link_to 'Admin', admin_users_path %> |
      <% end %>
       Signed in a <%= current_user.email %> 
        (<%= link_to 'Log out', sessions_path('current'), method: :delete %>)
    <% else %>
      <%= link_to 'Log in', new_sessions_path %> | <%= link_to 'Sign Up', new_users_path %>
    <% end %>
  </small>
</footer>